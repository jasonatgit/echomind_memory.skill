# config.example.yaml — EchoMind config template
# Copy to ~/.echomind/echomind_config.yaml and modify as needed

# ── Server config ──
server:
  host: "127.0.0.1"   # HTTP API listen address
  port: 8005          # HTTP API listen port
  api_key: ""         # API Key (empty=skip auth)